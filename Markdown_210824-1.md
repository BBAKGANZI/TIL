```sql
SELECT [DISTINCT] (field명1) ..... (field명..), 함수
  FROM tabel명
  WHERE 조건		# =, and, or, not, like, between, in
  JOIN table명
  ON JOIN조건
  GROUP BY field
  HAVING group by 조건
  ORDER BY field명.... [DESC]
  LIMIT 숫자 OFFSET 숫자

국가명, 도시명, 도시인구수 Top10

C create
R read, retrieve
U update
D delete
```

